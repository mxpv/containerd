name: Go
on:
  pull_request:
    branches:
    - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04
    steps:

    - name: Set up Go 1.12
      uses: actions/setup-go@v1
      with:
        go-version: 1.12

    - name: Checkout
      uses: actions/checkout@v1
      with:
        fetch-depth: 1
        path: go/src/github.com/containerd/containerd
   
    - name: Install dev tools
      run: ./script/setup/install-dev-tools
      env:
        GOPATH: /home/runner/work/woodpecker/go

    - name: Check
      run: make check
      env:
        GOPATH: /home/runner/work/woodpecker/go
        PATH: /home/runner/work/woodpecker/go/bin

    - name: Build
      run: make build
      env:
        GOPATH: /home/runner/work/woodpecker/go
